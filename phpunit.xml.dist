<?xml version="1.0" encoding="UTF-8"?>
<phpunit bootstrap="tests/bootstrap.php" colors="true" cacheDirectory=".phpunit.cache">
    <php>
        <ini name="memory_limit" value="-1"/>
        <server name="APP_ENV" value="test"/>
    </php>

    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <!-- Runs migrations against legacy-shaped rows in a scratch database; needs the MySQL server. -->
        <testsuite name="Migration">
            <directory>tests/Migration</directory>
        </testsuite>
        <!-- Needs a MySQL server and a prepared test database, see the "Application tests" CI job. -->
        <testsuite name="Application">
            <directory>tests/Application</directory>
        </testsuite>
    </testsuites>

    <extensions>
        <bootstrap class="Zenstruck\Foundry\PHPUnit\FoundryExtension"/>
        <bootstrap class="DAMA\DoctrineTestBundle\PHPUnit\PHPUnitExtension"/>
    </extensions>
</phpunit>
